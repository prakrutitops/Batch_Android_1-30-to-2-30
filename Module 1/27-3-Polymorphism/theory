Polymorphism:

1)Method Overloading:When two methods having a same name but number of parameters are diffrent
2)Method Overriding:When two methods having a same name but number of parameters are also same

